# Frontend

Per farlo partire digitare `docker compose up` e poi andare su `http://localhost:5173`

## Pagine attuali
1. Login `http://localhost:5173/login`
2. Register `http://localhost:5173/register`
3. Dashboard - Non finita `http://localhost:5173/dashboard`
4. 404 `http://localhost:5173/404`

> Sono già tutte con layout responsive.  
> Si dovrà, però, cambiare un po' la struttura delle directory per rendere il sito più efficiente possibile (fare fare pochi re-render). Attualmente è "buttato" lì giusto per fare il frontend.  
> Le route sono già correttamente impostate.